{
  "name": "plebly-fund",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-about.mjs && vite",
    "generate": "node scripts/build-about.mjs",
    "build": "npm run generate && node scripts/check-parameters-network.mjs && node scripts/build-sitemap.mjs && node scripts/build-llms.mjs && tsc && vite build && node scripts/spa-fallback.mjs && node scripts/build-og-pages.mjs",
    "preview": "vite preview",
    "test": "npm run generate && vitest run",
    "test:watch": "npm run generate && vitest"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "@types/qrcode": "^1.5.6",
    "happy-dom": "^20.11.1",
    "marked": "^15.0.7",
    "typescript": "^5.8.2",
    "vite": "^6.2.2",
    "vitest": "^3.0.9"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.3.1",
    "dompurify": "^3.4.13",
    "qrcode": "^1.5.4"
  }
}
