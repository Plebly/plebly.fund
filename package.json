{
  "name": "plebly-fund",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-about.mjs && vite",
    "build": "node scripts/build-about.mjs && node scripts/build-sitemap.mjs && tsc && vite build && node scripts/spa-fallback.mjs && node scripts/build-og-pages.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "happy-dom": "^20.11.1",
    "marked": "^15.0.7",
    "typescript": "^5.8.2",
    "vite": "^6.2.2",
    "vitest": "^3.0.9"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.3.1",
    "qrcode": "^1.5.4"
  }
}
